import type { TestRunnerConfig } from "@storybook/test-runner"

const config: TestRunnerConfig = {
  // Minimal configuration - just checks that stories render without crashing
}

export default config
